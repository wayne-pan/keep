{
  "name": "cursor",
  "detect": "command -v cursor || [ -d '$HOME/.cursor' ]",
  "config_path": "$HOME/.cursor/mcp.json",
  "config_format": "json",
  "mcp_key": "mcpServers",
  "config_template": {
    "command": "{{python}}",
    "args": ["{{server_path}}"],
    "env": {}
  }
}
